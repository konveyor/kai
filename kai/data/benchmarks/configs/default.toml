# Serves as an exmaple for benchmark testing

[models]
provider = "ChatIBMGenAI"
# Can either be located in data/templates or the full path of the template
# template = "custom_template_here.jinja" 

[models.args]
model_id = "mistralai/mixtral-8x7b-instruct-v01"

[incident_store]
provider = "in_memory"

[incident_store.args]
dummy = true
