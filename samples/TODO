Need to break out the fetch and analyze.

Prob would be good to have a configuration file and then load that
We expect folks to run the fetch script independent of analyze

Keep 1 script to show how to run kantra as an example, but clean up and remove the others


